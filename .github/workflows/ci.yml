name: CI

on:
  pull_request:
  push:
    branches:
      - main

jobs:
  test:
    name: Test
    runs-on: ubuntu-latest
    permissions:
      contents: read
      id-token: write
    steps:
      - name: Checkout repository
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - name: Set up pnpm
        uses: ./.github/actions/setup-pnpm
        with:
          bot-id: ${{ vars.TAKUMI_GUARD_BOT_ID }}
          cache: "true"

      - name: Run tests
        run: |
          pnpm install --frozen-lockfile
          pnpm run test

  lint:
    name: Lint
    runs-on: ubuntu-latest
    permissions:
      contents: read
      id-token: write
    steps:
      - name: Checkout repository
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - name: Set up pnpm
        uses: ./.github/actions/setup-pnpm
        with:
          bot-id: ${{ vars.TAKUMI_GUARD_BOT_ID }}
          cache: "true"

      - name: Run lint checks
        run: |
          pnpm install --frozen-lockfile
          pnpm run lint

  typecheck:
    name: Typecheck
    runs-on: ubuntu-latest
    permissions:
      contents: read
      id-token: write
    steps:
      - name: Checkout repository
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - name: Set up pnpm
        uses: ./.github/actions/setup-pnpm
        with:
          bot-id: ${{ vars.TAKUMI_GUARD_BOT_ID }}
          cache: "true"

      - name: Run type checks
        run: |
          pnpm install --frozen-lockfile
          pnpm run typecheck

  check-mcpb:
    name: MCPB Manifest Validation
    runs-on: ubuntu-latest
    permissions:
      contents: read
      id-token: write
    steps:
      - name: Checkout repository
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - name: Set up pnpm
        uses: ./.github/actions/setup-pnpm
        with:
          bot-id: ${{ vars.TAKUMI_GUARD_BOT_ID }}
          cache: "true"

      - name: Validate manifest.json
        run: |
          pnpm install --frozen-lockfile
          pnpm run check:mcpb

  license-check:
    name: Dependency License Check
    runs-on: ubuntu-latest
    permissions:
      contents: read
      id-token: write
    steps:
      - name: Checkout repository
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - name: Set up pnpm
        uses: ./.github/actions/setup-pnpm
        with:
          bot-id: ${{ vars.TAKUMI_GUARD_BOT_ID }}
          cache: "true"

      - name: Analyze unconfirmed licenses
        run: |
          pnpm install --frozen-lockfile
          pnpm run license:analyze

  build-container-image-test:
    name: Build Container Image Test
    runs-on: ubuntu-latest
    permissions:
      contents: read
      id-token: write
    steps:
      - name: Checkout repository
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - name: Setup Takumi Guard
        id: takumi-guard
        uses: flatt-security/setup-takumi-guard-npm@9a5d797c2085b6326d3a2985c08e3a114b9b88c1 # v1.1.1
        with:
          bot-id: ${{ vars.TAKUMI_GUARD_BOT_ID }}
          # The token is handed to Docker as a build secret, so nothing on the
          # runner needs an .npmrc. This also makes auth failures exit non-zero.
          set-registry: "false"

      - name: Validate Takumi Guard token
        env:
          TAKUMI_GUARD_TOKEN: ${{ steps.takumi-guard.outputs.token }}
        run: |
          if [ -z "$TAKUMI_GUARD_TOKEN" ]; then
            echo "::error::Takumi Guard authentication failed. TAKUMI_GUARD_TOKEN is empty."
            exit 1
          fi

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@bb05f3f5519dd87d3ba754cc423b652a5edd6d2c # v4.2.0

      - name: Build Docker image
        uses: docker/build-push-action@53b7df96c91f9c12dcc8a07bcb9ccacbed38856a # v6
        with:
          context: .
          file: ./Dockerfile
          platforms: "linux/amd64,linux/arm64"
          push: false
          secrets: |
            npm_auth=${{ steps.takumi-guard.outputs.token }}
        env:
          DOCKER_BUILD_SUMMARY: false
